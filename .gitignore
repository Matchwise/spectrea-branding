# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
tsconfig.tsbuildinfo
.playwright-mcp/
.claude/

# Ad-hoc screenshot dumps at repo root
/*.png

.superpowers/
.illustration-lab/

# Agent-run blackboards (audit charters, briefs, findings). Must never be
# committed: they catalogue exactly what a public repo should not carry, and
# the machine-global ignore that used to cover this does not travel with a
# clone (public-exposure audit 2026-08-13, findings A1-F3/A4-F5).
.runs/

# Generated internal-tier canon artefacts (trust masters, strategy fields).
# Handed off locally to the private consumer repos; never published.
internal/
