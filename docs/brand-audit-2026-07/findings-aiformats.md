# codex-W4 findings - AI consumability

## §1 Inventory Table

| ID | AI-facing surface | Format + token cost | Fetchability | Freshness / drift risk |
|---|---|---:|---|---|
| AIF-1 | `/llms.txt` | Markdown index + compact facts; 12,823 chars ~= 3,206 tokens, 98 lines (measured chars/4). | Repo has `public/llms.txt`; the app links it from Downloads with `href="/llms.txt"` and calls it the file that "points AI tools at the canonical docs" (`src/pages/resources/Downloads.tsx:49-59`); robots says AI crawler content lives at `/brand-guide.md` and `/llms.txt` (`public/robots.txt:1-3`). | Hand-maintained today: package scripts generate assets and PDF only (`package.json:10-12`), and `rg` found no `brand.ts` import/use in `scripts/generate-*.mjs` (measured). |
| AIF-2 | `/brand-guide.md` | Long Markdown guide with tables, code, and image references; 53,598 chars ~= 13,400 tokens, 814 lines (measured chars/4). | Repo has `public/brand-guide.md`; Downloads links it at `/brand-guide.md` (`src/pages/resources/Downloads.tsx:21-30`); `llms.txt` points to it as the LLM-readable mirror (`public/llms.txt:17-20`). | Mixed authority: `brand.ts` says "Single source of truth" (`src/data/brand.ts:1-2`), but the guide says updates live in the Markdown and downstream consumers are regenerated from it (`public/brand-guide.md:680`), then says the live app wins if guide and app disagree (`public/brand-guide.md:813`). |
| AIF-3 | `src/data/brand.ts` | TypeScript `as const` data module; 24,088 chars ~= 6,022 tokens, 447 lines (measured chars/4). | Fetchable only to repo-aware coding agents, not from the deployed public site; the file exports the brand object and canonical fields (`src/data/brand.ts:6-41`, `src/data/brand.ts:285-392`, `src/data/brand.ts:418-438`). | Canonical by local file header (`src/data/brand.ts:1-2`) and consumed by multiple app pages (`src/pages/foundation/Voice.tsx:13-171`, `src/pages/foundation/Positioning.tsx:44-52`), but current public AI files are not derived from it (AIF-1). |
| AIF-4 | Live app `/` + app routes | Runtime SPA; `index.html` shell is 1,216 chars ~= 304 tokens, while `src/pages/**/*.tsx` source totals 501,237 chars ~= 125,309 tokens (measured chars/4). | Root renders a JS app shell (`index.html:20-21`) and React uses `HashRouter` (`src/main.tsx:7-12`); actual pages are route components (`src/App.tsx:53-86`). Non-JS agents fetching clean paths like `/foundation/voice` from `llms.txt` (`public/llms.txt:46-75`) will not receive page-specific static Markdown. | Freshness varies: many pages import `brand.ts` values (`src/pages/foundation/Voice.tsx:13-171`), but some page facts are hardcoded to match `brand.ts` rather than derived (`src/pages/color/PaletteExplorer.tsx:4`). |
| AIF-5 | `/brand-guide.pdf` | Binary PDF, 1,465,805 bytes measured; text payload should approximate the Markdown guide because it is generated from `public/brand-guide.md`. | Repo has `public/brand-guide.pdf`; Downloads links `/brand-guide.pdf` (`src/pages/resources/Downloads.tsx:35-44`); `llms.txt` lists it as printable same-content guide (`public/llms.txt:20`). | Generated, but from Markdown rather than `brand.ts`: `generate-pdf.mjs` declares "Generate public/brand-guide.pdf from public/brand-guide.md" (`scripts/generate-pdf.mjs:3`), reads `brand-guide.md` (`scripts/generate-pdf.mjs:32`, `scripts/generate-pdf.mjs:209`), and writes a footer saying it was generated from `/public/brand-guide.md` (`scripts/generate-pdf.mjs:280`). |
| AIF-6 | `/brand-assets/*.svg` | SVG visual references; 19 files, 66,071 chars ~= 16,518 tokens if read as text (measured chars/4). | `llms.txt` exposes the asset URLs for logos, swatches, gradients, and type samples (`public/llms.txt:22-42`). | Generated public artifacts exist, but constants are re-declared in the generator (`scripts/generate-brand-assets.mjs:53-70`) and individual SVGs are written from that script (`scripts/generate-brand-assets.mjs:226-257`, `scripts/generate-brand-assets.mjs:372-497`). |
| AIF-7 | `/robots.txt` | Robots policy; 578 chars ~= 144 tokens, 34 lines (measured chars/4). | Public file explicitly allows all agents and named AI crawlers, then declares `Sitemap: /sitemap.xml` (`public/robots.txt:5-33`). | Hand-maintained policy only; it helps crawler access but does not solve rule selection or brand correctness (`public/robots.txt:1-3`). |

## §2 Failure->Gap Matrix

| ID | Failure shape | Gap that permits it | Evidence | Machine-format implication |
|---|---|---|---|---|
| AIF-8 | Wrong category noun. | (f) duplicated formats can disagree; (d) no generated contract. | The brief records the actual lag case until `4d4c804` (`brief-worker-aiformats.md:27`); current canonical category is in `brand.ts` (`src/data/brand.ts:31-40`) and manually restated in `llms.txt` (`public/llms.txt:82`). | Category must be emitted from one source into both `llms.txt` and a contract field such as `positioning.category`. |
| AIF-9 | Hype vocab such as "AI-powered" or "seamless". | (f) formats disagree; (d) no banned-term contract. | `brand.ts` bans 12 terms including `AI-powered`, `seamless`, `cutting-edge`, `synergy`, and verb-form `leverage` (`src/data/brand.ts:332-335`); the Markdown guide bans only the first 8 terms (`public/brand-guide.md:125`); `llms.txt` lists privileged words but no banned list (`public/llms.txt:94`). | The contract needs `copy.neverUse[]`, and generated docs must render the same list from it. |
| AIF-10 | Chirpy or vague error message. | (c) examples not present in compact AI surface; (e) right examples sit in the long guide. | `brand.ts` gives the correct direct error and the bad "Oops!" version (`src/data/brand.ts:362-365`); the guide repeats the contrastive pair (`public/brand-guide.md:107-114`); `llms.txt` gives only the formula/facets (`public/llms.txt:91-94`). | Few-shot pairs need to be split into a small loadable file or embedded in a task-specific agent snippet. |
| AIF-11 | Totalizing claims that imply AI replaces judgment or works as a black box. | (b) rule exists but is not in the compact AI surface; (d) no testable constraints. | `brand.ts` says AI assists and never replaces judgment (`src/data/brand.ts:138-140`) and anti-values include "Never replaces human judgment" plus "Never a black box" (`src/data/brand.ts:164-180`); the guide has the same anti-values (`public/brand-guide.md:78-82`); `llms.txt` does not expose anti-values in key facts (`public/llms.txt:77-97`). | Contract should include `antiValues[]` and generated checklist checks for replacement/black-box claims before output. |
| AIF-12 | Wrong casing/naming such as "Spectrea AI". | (b) rule buried in Naming section; (f) compact file names only a subset. | The guide forbids `"Spectrea AI" (lead)` (`public/brand-guide.md:133-139`); `llms.txt` only says not "Spectre", "Spectra", or "SpectreAI" (`public/llms.txt:79`) and separately summarizes feature naming (`public/llms.txt:96`). | Contract should separate `brandName.never[]` from `featureNaming` so validators can catch both company-name and feature-name errors. |
| AIF-13 | Off-palette hex or wrong text tier. | (d) no machine-readable color schema; (e) details are too spread out for a working context. | `brand.ts` has selected palette colors and dark-mode fields (`src/data/brand.ts:417-438`); the guide defines readable text tiers and Tailwind mappings (`public/brand-guide.md:300-324`); `llms.txt` summarizes neutrals but has no utility mapping or allowed-use schema (`public/llms.txt:83-86`). | Contract needs `colors.tokens[]`, `colors.textTiers[]`, `colors.darkMode`, and `tailwindMap`; checklist can reject unknown hexes and `Pewter` for informational text. |
| AIF-14 | Forbidden gradient use: skip-pair duet, buttons, text, borders. | (b) rule stated outside `llms.txt`; (d) no usage enum. | The guide says lockup gradient is only inside `LogotypeGradient` (`public/brand-guide.md:421-426`), duets must be adjacent (`public/brand-guide.md:435-439`), and gradients are never for buttons/text/borders/small repeated elements (`public/brand-guide.md:445-447`); `llms.txt` only lists the brand and lockup gradients (`public/llms.txt:88-89`). | Contract should model `gradients.allowedUses`, `gradients.neverUses`, and `duets.allowedPairs`. |
| AIF-15 | Wrong lockup or standalone `pectrea` treatment. | (b) anatomy rule buried; (d) no logo constraints. | The guide says `pectrea` is only the tail inside canonical `Logotype`/`LogotypeGradient`, and sentence-case `Spectrea` is used everywhere else (`public/brand-guide.md:163-166`); it also says the lockup has exactly two forms and no gradient wordmark (`public/brand-guide.md:174-183`). | Contract needs `logo.wordmark.tailOnlyInCanonicalLockup`, `logo.lockupForms`, and generated agent rules for logo/copy tasks. |
| AIF-16 | Privileged-word overdose. | (b) density rule outside `llms.txt`; (d) no countable constraint. | `brand.ts` defines privileged words (`src/data/brand.ts:318-331`) and the density rule (`src/data/brand.ts:350-352`); the guide repeats density (`public/brand-guide.md:121-127`); `llms.txt` lists privileged words but not the density limit (`public/llms.txt:94`). | Contract should include a countable density rule: max 2 privileged words per paragraph and max 1 `living`/`alive` per page. |
| AIF-17 | Outcome-vs-mechanism hero copy or buyer copy that leads with brand fog. | (c) contrastive examples not loaded; (b) surface-specific rule is prose. | `brand.ts` says buyer surfaces lead with proof and product copy warms through specificity (`src/data/brand.ts:338-347`); tone examples show right vs wrong marketing and feature-announcement patterns (`src/data/brand.ts:356-371`); the guide says marketing starts with what was hidden, then grounds and equips (`public/brand-guide.md:93-100`). | A task checklist should require surface classification before generation: buyer, product, trust, docs, social, or beginner docs. |
| AIF-18 | Wrong dot count, colored dots, or squircle container in generated logo/imagery. | (b) rule is in logo section/misuse section; (d) no visual contract. | The guide defines 10 dots, grey primary dots, trailing-dot behavior, and circle-not-squircle container (`public/brand-guide.md:168-172`); misuse forbids changing dot count, squircle containers, and colored primary dots (`public/brand-guide.md:239-242`). | Contract should include `logo.dotCount=10`, `logo.containerShape=circle`, and `logo.primaryDots=#A3A3A3`; prompt snippets should expose these before image/logo generation. |
| AIF-19 | Agent follows live-page links and gets app shell or wrong route behavior. | (e) live app is not a static text reference; fetchability differs by agent. | `llms.txt` lists clean route URLs (`public/llms.txt:44-75`), but the deployed app shell is `index.html` plus JS (`index.html:20-21`) and the app uses `HashRouter` (`src/main.tsx:7-12`). | `llms.txt` should label app routes as JS-rendered previews and prioritize `/brand-guide.md`, `/brand-contract.json`, and examples for non-JS agents. |
| AIF-20 | AI cannot tell which source wins. | (f) authority conflict across AI-facing surfaces. | `brand.ts` declares itself single source (`src/data/brand.ts:1-2`), `llms.txt` says the live app is canonical and Markdown exists for LLM/readable print use (`public/llms.txt:19`), the guide says Markdown updates are the source for downstream consumers (`public/brand-guide.md:680`), and the guide footer says app wins on disagreement (`public/brand-guide.md:813`). | Generated AI formats need an explicit `sourceOfTruth` stanza: canonical data = `src/data/brand.ts`; public artifacts = derived. |

## §3 llms.txt Audit

| ID | Field | Current hardness | Missing or loose vs `brand.ts` + guide | Required verifiable shape |
|---|---|---|---|---|
| AIF-21 | Discovery and load order | Good discovery surface: points at Markdown/PDF, assets, pages, and key facts (`public/llms.txt:17-97`). | It does not tell non-JS agents that clean page links are SPA routes under `HashRouter` (`src/main.tsx:7-12`) or that Markdown/contract should be loaded before route previews. | Add `load_order`: `brand-contract.json`, `agent-rules.md`, `few-shots.md`, `brand-guide.md`, then JS app. |
| AIF-22 | Identity/positioning | Includes name, pronunciation, tagline, category, colors, and feature naming (`public/llms.txt:79-97`). | It omits the `"Spectrea AI" (lead)` negative (`public/brand-guide.md:139`) and has no generated proof that category matches `brand.ts` (`src/data/brand.ts:31-40`). | Add generated fields `brand.name`, `brand.neverNames[]`, `positioning.category`, `positioning.promise`. |
| AIF-23 | Voice/copy | Includes formula, facets, and privileged words (`public/llms.txt:91-94`). | It omits `neverUse[]` (`src/data/brand.ts:332-335`), density (`src/data/brand.ts:350-352`), context shifts (`src/data/brand.ts:338-347`), and contrastive pairs (`src/data/brand.ts:354-389`). | Add `copy.neverUse[]`, `copy.privileged[]`, `copy.density`, `copy.surfaceRules[]`, and link/generated few-shot pairs. |
| AIF-24 | Color | Includes spectrum, neutrals, default light mode, dark tokens, and dark washes (`public/llms.txt:83-86`). | It lacks a schema for allowed hexes, text-tier roles, utility mappings, and "Pewter is whisper only" enforcement (`public/brand-guide.md:300-324`). | Add `colors.tokens[]` with `hex`, `role`, `contrast`, `allowedUse`, `darkMode`, and `tailwindMap`. |
| AIF-25 | Gradients | Includes primary and lockup gradients (`public/llms.txt:88-89`). | It omits adjacent-pair rule, `#6FB884` bridge requirement, and never-use cases for buttons/text/borders (`public/brand-guide.md:421-447`). | Add `gradients.primary`, `gradients.lockupOnly`, `gradients.allowedPairs`, `gradients.neverUses`. |
| AIF-26 | Logo | Lists approved SVG assets and says lockup wordmark stays monotone (`public/llms.txt:24-30`, `public/llms.txt:89`). | It omits dot count, trailing dots, circle-not-squircle, `pectrea` tail-only treatment, two lockup forms, clear-space, and misuse list (`public/brand-guide.md:163-183`, `public/brand-guide.md:239-242`). | Add `logo.constraints` with countable fields and forbid-list. |
| AIF-27 | Illustration | Summarizes v4 prompt, tint discipline, no outlines, and hand-SVG primitive scope (`public/llms.txt:13`, `public/llms.txt:95`). | It references `docs/illustration-prompt.md` (`public/llms.txt:13`), but deployment uploads `dist` only (`.github/workflows/deploy.yml:28-30`); the full eight-item checklist and reject conditions are in the guide, not the compact surface (`public/brand-guide.md:547-560`). | Add public `illustration-checklist.md` or generated contract fields for checklist/rejects. |
| AIF-28 | Freshness | It has current v2/v4 labels in prose (`public/llms.txt:5`, `public/llms.txt:95-97`). | It has no generated timestamp, source path, source hash, or pointer proving it was emitted from `brand.ts`; current scripts expose only assets/PDF generation (`package.json:10-12`). | Generated header should say `source: src/data/brand.ts`, `generatedAt`, and `doNotEdit`. |

## §4 Derivation-Infra Ground Truth

| ID | Ground truth | Evidence | Implication for `generate:ai` |
|---|---|---|---|
| AIF-29 | Current public derivation exists for assets and PDF only. | `package.json` has `generate:assets`, `generate:pdf`, and `generate:all`, but no AI-format script (`package.json:10-12`). | Add `generate:ai` and include it in `generate:all` only after it is deterministic and read-only from canonical data. |
| AIF-30 | Asset generation proves the repo can emit public static artifacts, but it re-declares facts. | `generate-brand-assets.mjs` writes into `public/brand-assets` (`scripts/generate-brand-assets.mjs:22-25`) and re-declares color constants (`scripts/generate-brand-assets.mjs:53-70`). | Do not copy this pattern for AI formats; emitting copied constants would recreate AIF-8/AIF-9 drift. |
| AIF-31 | PDF generation is derived from Markdown, not canonical brand data. | `generate-pdf.mjs` sets `mdPath` to `public/brand-guide.md` and `pdfPath` to `public/brand-guide.pdf` (`scripts/generate-pdf.mjs:30-33`), reads the Markdown (`scripts/generate-pdf.mjs:209`), and writes a generated-from-Markdown footer (`scripts/generate-pdf.mjs:280`). | `generate:ai` should not parse the PDF or Markdown as the source; Markdown can be an output peer. |
| AIF-32 | Existing `.mjs` scripts do not import `brand.ts`. | `rg` for `src/data/brand`, `brand.ts`, `selectedPalette`, `voice`, `positioning`, and `import .*brand` in `scripts/generate-*.mjs` returned no matches (measured); the generator comments instead say logo geometry mirrors component code (`scripts/generate-brand-assets.mjs:70`, `scripts/generate-brand-assets.mjs:257`). | The new script must add a real data-loading strategy rather than assuming current generators solved TS import. |
| AIF-33 | Direct Node import of `brand.ts` is not currently supported by emitted JS. | The package is ESM (`package.json:5`), TypeScript is installed (`package.json:31`), but `tsconfig.json` has `noEmit: true` and includes `src` only (`tsconfig.json:2-20`). | Feasible paths: use the TypeScript compiler API to transpile/evaluate `src/data/brand.ts` in-memory, run a dedicated build-to-temp step, or move canonical brand data to importable JSON/JS while keeping TS types derived. |
| AIF-34 | Some app pages are already derived from `brand.ts`, but public AI artifacts are not. | Voice page renders `voice.formula`, `voice.alwaysUse`, `voice.neverUse`, and `voice.vocabularyDensity` from `brand.ts` (`src/pages/foundation/Voice.tsx:13-171`); the Downloads page says `brand-guide.md`/`llms.txt` are AI-readable files (`src/pages/resources/Downloads.tsx:65-71`). | `generate:ai` can reuse canonical data for copy/tone fields immediately, while guide/page hardcoded content needs later convergence. |

## §5 Candidate Assessment + Draft Recommendation

| ID | Candidate | Kills which failures | Token cost | Maintenance path | Consumption story |
|---|---|---|---:|---|---|
| AIF-35 | Hardened generated `llms.txt`. | AIF-8, AIF-19, AIF-20; partially AIF-9/AIF-12 by exposing hard negatives. | Target 4k-5k tokens; current is 3,206 measured. | Generated from `brand.ts` plus a small route manifest; never hand-edit. | Root discovery file loaded first by web agents; tells agents which files to load for copy, UI, logo, imagery. |
| AIF-36 | Compact `brand-contract.json`. | AIF-8, AIF-9, AIF-12, AIF-13, AIF-14, AIF-15, AIF-16, AIF-18, AIF-20. | Target 2k-4k tokens depending on schema verbosity. | Derived from `brand.ts` fields (`src/data/brand.ts:31-40`, `src/data/brand.ts:285-392`, `src/data/brand.ts:418-438`) plus generated logo/gradient constraints currently only in guide. | Agents and CI can parse it; validators can reject banned terms, unknown hexes, invalid gradients, and logo misuse. |
| AIF-37 | Drop-in agent-rules snippet (`AGENTS.md`/`CLAUDE.md` style). | AIF-9, AIF-10, AIF-12, AIF-16, AIF-17, AIF-19. | Target 1k-2k tokens. | Generated from the contract and tone examples; flag any hand-maintained vendor copy as a new drift source. | Other repos vendor the snippet so coding agents see it without browsing the brand site. |
| AIF-38 | Few-shot on/off-brand contrastive pairs. | AIF-10, AIF-17, AIF-9, AIF-16. | Target 2k-3k tokens if generated from six existing pairs. | Directly derived from `voice.toneExamples` (`src/data/brand.ts:354-389`); later pairs must land in canonical data first. | Copy tasks load it when generating marketing, docs, errors, announcements, social, or beginner docs. |
| AIF-39 | Generation-time checklist. | AIF-11, AIF-13, AIF-14, AIF-15, AIF-16, AIF-18. | Target <1k tokens. | Generated from `brand-contract.json`; no separate prose ownership. | Agent runs it before final output: surface classified, banned terms absent, density checked, colors/gradients/logo constraints checked. |

| ID | Recommendation | Rationale |
|---|---|---|
| AIF-40 | Rank 1: implement `generate:ai` that emits `public/brand-contract.json` and `public/brand-checklist.md` from canonical data. | This is the only candidate that makes failures verifiable instead of descriptive; it directly fixes stale noun drift, banned terms, color/gradient/logo constraints, and density. |
| AIF-41 | Rank 2: regenerate `public/llms.txt` as a router, not the whole brand brain. | `llms.txt` should stay small, root-fetchable, and explicit about load order; it should point to contract/checklist/few-shots and label JS routes as previews. |
| AIF-42 | Rank 3: generate `agent-rules.md` and `few-shots.md` as task-specific companions. | Rules make off-repo coding agents behave; few-shots carry tone taste that JSON cannot, using existing canonical examples. |
| AIF-43 | Reject any format that is hand-maintained separately from `brand.ts`. | Current drift is caused by manual restatement across `llms.txt`, `brand-guide.md`, pages, and scripts; another hand-maintained AI artifact would widen AIF-20, not fix it. |

## Residuals

| ID | Residual |
|---|---|
| AIF-44 | I did not live-fetch the deployed site; fetchability findings are grounded in repo deployment shape: `dist` upload (`.github/workflows/deploy.yml:28-30`), Vite base `/` (`vite.config.ts:4-7`), `index.html` shell (`index.html:20-21`), and `HashRouter` (`src/main.tsx:7-12`). |
| AIF-45 | I did not extract text from the PDF; its AI-token estimate is inferred from the generator reading `public/brand-guide.md` (`scripts/generate-pdf.mjs:209`) and writing the PDF from that content (`scripts/generate-pdf.mjs:292-313`). |
| AIF-46 | I did not audit `docs/illustration-prompt.md` because it is outside this worker's scoped file list (`brief-worker-aiformats.md:12-16`); I only flag that `llms.txt` references it (`public/llms.txt:13`) while deployment uploads `dist` (`.github/workflows/deploy.yml:28-30`). |

## Breadth Gate

| ID | Gate |
|---|---|
| AIF-47 | What in scope am I not looking at? Covered all scoped surfaces named in the brief: `llms.txt`, `brand-guide.md`, `brand.ts`, `robots.txt`, deploy workflow, generate scripts, `index.html`, and `src/pages/**` routing/app implications (`brief-worker-aiformats.md:12-16`); remaining uncertainty is live-network state and out-of-scope illustration prompt content. |

## Lens Verdict

| ID | Verdict line |
|---|---|
| AIF-48 | The root cause is not that the guide is absent; it is that AI-facing rules are prose-heavy, duplicated, and inconsistently exposed. |
| AIF-49 | The highest-risk omissions in `llms.txt` are banned terms, density, contrastive tone pairs, gradient never-uses, logo constraints, and source-of-truth authority. |
| AIF-50 | The current public AI path works for discovery but not for enforcement. |
| AIF-51 | The live app is useful for humans, but hash routing makes it a weak primary reference for non-JS agents. |
| AIF-52 | The existing generators prove static artifact generation is acceptable, but they also show the current anti-pattern: re-declared constants instead of canonical import. |
| AIF-53 | The winning wave-2 design should be a generated bundle: contract for verification, checklist for runtime behavior, few-shots for taste, and hardened `llms.txt` for routing. |

## DRAFT-amendment proposal

| ID | Amendment |
|---|---|
| AIF-54 | Add `npm run generate:ai` to emit `public/brand-contract.json`, `public/brand-checklist.md`, `public/brand-few-shots.md`, and a regenerated `public/llms.txt` from canonical brand data; each generated file should start with `DO NOT EDIT - generated from src/data/brand.ts`. |
| AIF-55 | Extend canonical data so every rule currently only in `brand-guide.md` but needed by AI has a typed source field: `brand.neverNames`, `copy.neverUse`, `copy.density`, `colors.textTiers`, `colors.tailwindMap`, `gradients.neverUses`, `logo.constraints`, and `illustration.rejects`. |
| AIF-56 | Change public guidance so `llms.txt` says: load `brand-contract.json` for hard constraints, `brand-few-shots.md` for copy tone, `brand-checklist.md` before generation, and `brand-guide.md` for full human context. |
