# Ratification session — 2026-08-06 (Darren, interview after audit convergence)

Decisions captured one at a time via /interview; evidence artifact: https://claude.ai/code/artifact/fca48c8d-8e26-47be-b42c-2c62f6211c52

## Decided

1. **Release sequencing (D37/D17):** hold the push; fold the cheap high-severity fixes from this session into main, then push v2.4.x once. Downstream re-pin + AGENTS.md URL fix follow the push.
2. **brand-guide.md governance (D16/D38):** HYBRID generation — data-bearing values (weights, sizes, hexes, ratios, clearspace) move into brand.ts and those guide sections become generated; prose stays hand-written. Programme work, not a pre-push gate.
3. **Wordmark weight (D38):** Albert Sans **600** canonical. Pre-push doc fix: correct brand-guide.md:534 ("700 Bold — logo wordmark, hero only" parenthetical) and TypeExplorer.tsx:142/:181; canonize the value in brand.ts. No asset regeneration needed (SVGs + component already render 600).
4. **Lockup constants ("B") ratified:** rendered stroke 0.113em (= 'l' stem at 600), mark ink height 0.714em (= real S glyph), mark→text ink gap 0.131em (= S rsb + 0.02em tracking + p lsb), dots keep 7:8 diameter:stroke. Replaces the hard-coded 0.72em/8-unit/0.05em constants. Requires: brand.ts lockup-constants block, SpectreaLogo.tsx update, regeneration of lockup SVGs, parity check between component and generator.
5. **Mark geometry: K3′ adopted.** S-fit spine and top terminal (3 G1-continuous cubics fitted to the Albert Sans 600 S medial axis, closest-point-paired centerline) with the shallow storytelling tail (exit 35°, trailing dots level in the terminal band).
   - Authoritative coordinates: **`k3-prime-ratified.json` in this run directory** (segs + B constants + dot rule) — copy into brand.ts when implementing, then re-derive the 10 dots at i/9 arc fractions.
   - Measured: deviation from S spine 2.4% of glyph height (all of it the deliberate tail); counter mouth 18.9%; readable at 11px in the mono ladder.
   - K3″ (top-extended) evaluated and REJECTED: mouth 15.2%, deviation 3.4% — asymmetry (restrained top, shallow tail) is deliberate and ratified.

6. **Metaphor hierarchy (D24): explicitly CO-PRIMARY.** Prism = identity/reveal role, Living Network = product/growth role. Implementation: restructure `brand.ts` visualMetaphor from primary/secondary to co-primary-with-roles, regenerate AI artifacts, rewrite brand-guide.md:68 to match. Open implementation detail (Darren may refine): art-direction tiebreaker when a surface needs a single lead metaphor.

7. **Propagation scope (D26/D19/D18): RENDER EVERYTHING.** The SPA gains sections for all post-2026-07 canon: four trust masters, three AI registers, seven surface patterns, nine unrendered exports. Standing rule: anything in canon must render somewhere in the guide. Naming.tsx corrected to the ratified register taxonomy (unconditional). Follow-up ratification round (needs Darren reading the texts): pick the one canonical hero-open of the five (D28) and confirm the outcome-first rewrite of the marketing exemplar (D27 — I draft, he ratifies).

8. **Contrast policy (D10/D11/D34): FLOORS + RATIFIED EXCEPTIONS.** AA text / 3:1 UI-and-focus are the system defaults; a canonical exception registry (name, rationale, usage bounds) holds any deliberate departures. Unconditional fixes: correct the false "all four accents pass AA on Ink" claim (×3 sites, Cobalt 3.93:1), correct the palette tool's pass threshold, canonize the on-wash text hexes (D36). Follow-up ratification round: which exceptions are granted (Amber focus ring scope, sub-AA button styles) — each entry Darren-ratified with measured values.

9. **Hero anchor (P4): option A — honest caption now, regenerate later.** Pre-push fix: the guide caption stops overclaiming a full checklist pass and states the anchor's confirmed breaches (all four primaries at 96–100% canonical saturation against the prompt's explicit reject condition; neutral-grey supporting forms ~5.7% of frame against the neutral-fill rule). Regeneration of the anchor — plus a second anchor — happens in a dedicated art session, not pre-push.

10. **Illustration process (D4/D6/D7/D32/D33): abstraction ladder with one style DNA.** The style system becomes explicitly content-agnostic via three declared abstraction levels — **L1 abstract/spot** (ambient, decorative, metaphor-forward), **L2 metaphoric scene** (people + brand metaphors, the current anchor's register), **L3 concept/product** (concrete UI, diagrams, product moments) — sharing a single style DNA (palette treatment, texture, line/shape language, craft rules). Each level gets its own content brief and a quantified checklist (measurable saturation/coverage/composition bounds, like the existing prompt's reject conditions but per level). Prototyping the L1/L3 exemplars joins the art session alongside anchor regeneration.

11. **Small-size mark (critic risk a): NO micro-construction — the standard mark scales down unchanged.** Four candidates (M1 enlarged trailing pair, M3 optical bolding, M4 dotted S, M5 single spark) were built, measured and rejected: all judged too far from what the actual icon looks like. No canon amendment; misuse rules stand as written. The plain-S reading at favicon sizes (dots fusing below ~24px) is accepted as-is. Evidence retained on the artifact page (Micro-construction candidates panel, marked rejected).

12. **R8 "compounding intelligence" (VM-73): DEFEND — keep the phrase.** Darren's rationale: it is not the sole USP — the draft homepage (spectrea-web `ProductOverview.tsx`) carries five differentiator beats (persistent memory, provenance, collective intelligence, composability, the closed loop "Act, learn, repeat. It compounds."), so contest risk is spread across a portfolio, and Spectrea's version stays mechanism-tied (the graph) per the standing guardrail at brand.ts:201. Implementation: add a ratificationLedger entry recording the defend stance; D31's fix adds the mechanism to the three bare uses. Verified: the homepage compounding beat already states its mechanism ("Every outcome feeds back into the graph…") — guardrail-compliant.

13. **R9 trustCopy counsel review: caveat stays as the standing control, plus an explicit trigger in canon.** The masters remain brand-voice-only; new canonical rule: before any launch that puts trust/security/compliance claims on a public page, counsel reads the masters first. No counsel spend now; residual closes as controlled, trigger-based. Implementation: extend the counselNote at brand.ts:710 with the trigger condition, regenerate AI artifacts.

14. **D30 name hygiene: knockout search now, formal screen at the counsel trigger.** (a) Etymology: rewrite the "Latin" claim as a sourced coinage (Spectrea coined from *spectrum*, Latin, from *specere* "to look") across brand.ts:20, Naming.tsx:36, brand-guide.md:9/182 — fix-programme item. (b) PRISM echo: resolved by decision 6 (Prism re-ratified as co-primary metaphor; metaphor-in-product ≠ company name). (c) Trademark: immediate knockout search (web + register recon for confusingly similar marks in the AI/knowledge space); formal professional clearance rides the decision-13 counsel trigger — one engagement covers trust copy + clearance before public launch.
    - **Knockout search executed 2026-08-07 (web recon, US-indexed): NO BLOCKER FOUND.** Zero exact-match "Spectrea" companies, products, or trademark records surfaced across four queries (engines auto-correct to "Spectre" — evidence of distinctiveness). Adjacent landscape: several SPECTRE marks in remote classes (law-enforcement training, footwear, video games, firearms); Spectre AI / spectreai.io (crypto market intelligence, Google Startups-backed) is the closest phonetic neighbour but a different category; Spectro Cloud (K8s/AI infra), Spectora (home inspection), Spectra Analytics, SpectralAI — none in enterprise knowledge-graph space, none using the -ea coinage. Limits: recon only, US-indexed web; class/jurisdiction register search (incl. SG/EUIPO) belongs to the counsel screen.

15. **Decision record: committed to `docs/brand-review-2026-08/`** (ratifications, run ledger with final synthesis, K3′ geometry JSON, decision-sheet HTML + README index), mirroring the July precedent — the git-ignored `.runs/` blackboard stays the working copy. **Naming doc boundary (Darren):** `docs/naming-proposal-validated.md` is kept separate and independent from the branding system — it is history, not doctrine; it is not folded into the guide, canon, or generation, and brand fixes touch only the brand guide/canon claims derived from it (e.g. the etymology line), never the doc itself.

## Discovered during this session (add to fix programme)

- **B-solve correction (2026-08-07, from the Codex critic's fix-wave gate):** the decision-sheet's fixed-point solve measured the mark's ink box as if the whole spine were stroked, inflating it by the unstroked tail — rendered visible ink was ≈0.695em, not the ratified 0.714em. Re-solved on the VISIBLE ink envelope (stroked arc + dot disks): native strokeWidth 9.347→9.0531, dotRadius 4.089→3.9607, inkExtents L10.339/R50.828/T2.338/B59.540. Both B constraints now hold exactly (stroke 0.113em, visible ink 0.714em). The em-level B constants (the ratified authority) are unchanged.
- **Generator drops kerning + tracking** (generate-brand-assets.mjs:44-49 uses raw advanceWidth): shipped lockup SVGs' wordmark is 0.144em narrower and unkerned vs the SPA (SpectreaLogo.tsx:459 applies 0.02em). Fix generator to use layout positions + tracking when regenerating under B. **LANDED** (verified 2026-08-11): `generate-brand-assets.mjs` `textToPath()` walks fontkit's laid-out `run.positions[i].xAdvance` — kerning applied — and adds `canon.logo.lockup.trackingEm` per glyph.
- **Lockup alignment rule** must be defined on stroke ink, not bounding box (leftmost element is a near-invisible grey dot). **LANDED** (verified 2026-08-11): the generator places the wordmark pen at ink-right + 0.0643em (`generate-brand-assets.mjs:353`), i.e. off the ink envelope, not the bounding box.
- Critic risks (independent design critic, accepted as programme items): (a) no small-size mark carries the dot idea — mono contained variant fuses dots at favicon sizes; needs a canon amendment (micro-construction or grey dots retained) which current misuse rules forbid. Candidates built and measured on the artifact page (Micro-construction candidates panel): M1 enlarged trailing pair (pair survives at 16px, gaps ≥1px), M3 optical bolding (fails — spacing not weight), M4 dotted S (loses the S), M5 single spark (clean but pair→lone dot). RESOLVED as decision 11: all rejected, standard mark scales as-is; (b) mark grey #A3A3A3 on Canvas ≈ 2.48:1, below the brand's own 2.85:1 whisper floor — the metaphor carriers are the least visible element; (c) verify B optical weight on light AND dark before locking (monoline vs modulated glyph); (d) measurement discipline: the critic's aperture table (K3 11.6% vs 6.5%) did NOT replicate under direct geometry — corrected on the artifact page; decisions rest on replicated numbers only.

## Follow-up round ratifications (2026-08-07, post-push)

16. **Focus ring (D11, exception round 1 of 2): FIX, not exception — light surfaces switch to Amber active `#A86E00`.** Measured (blended): the shipped universal ring `rgba(225,144,0,0.7)` was 1.90:1 on Canvas and 1.81:1 on Cloud — below the ratified 3:1 UI floor — and 4.00:1 on Ink (passing). Three candidates rendered on the decision sheet (Focus ring panel): grant-as-is, solid Amber active in light, glow + 1px solid edge; middle shade Amber hover `#C58200` pre-rejected (2.90:1 on Cloud). Darren picked the recommended solid `#A86E00` (4.21:1 Canvas, 3.89:1 Cloud); dark mode keeps the soft alpha ring. The exception registry stays empty; its pending-grant note now lists only the button-text candidate. Implemented as v2.5.1: `brandTokens.focusRing` split light/dark, `.btn-focus`/`.btn-focus-dark` in index.css, Buttons.tsx demos select ring by surface, guide + ledger updated, artifacts regenerated. Codex critic gate: round 1 revise (dark-panel buttons inherited the light ring — fixed), round 2 **accepted** with independent measurements (verdict: `.runs/2026-08-06-brand-review/focus-ring-review/verdict.md`).

17. **Button labels (D34, exception round 2 of 2): GRANT — white labels stay everywhere; measurement doctrine updated.** WCAG 2.x reads white on Rose/Teal/Amber at 3.67/2.56/2.56 (only Cobalt passes, 4.51) and had my recommendation at Ink-label swaps. Darren challenged the ranking on perceptual grounds ("option 1 looks higher contrast, and it's more consistent") and asked for a better scale. APCA-W3 re-measurement (decision sheet, APCA addendum panel) reversed the verdict: white beats or ties Ink on every fill (Lc 76.4/68.4/54.6/54.9 vs Ink 31.7/39.6/53.0/52.7); the nominally compliant Ink-on-Rose swap would have been perceptually *worse* (39.6 < 45 spot minimum). Ratified as the registry's first granted entry `semantic-button-labels-white` (both metrics + bounds recorded), plus a canonical measurement doctrine (`accessibility.measurement`): WCAG 2.x AA stays the conformance floor, APCA-W3 Lc is the perceptual adjudicator, exception grants log both. Implemented as v2.5.2. Side observation queued for D40: dark-mode Ink-on-lightened-Cobalt hover is APCA-weak (40.5) despite passing WCAG 4.5:1.

18. **Hero-open (D28 part 1): ONE canonical rule — outcome-first fused with the reveal register, kept simple and adaptable (Darren's constraint).** The five competing prescriptions (guide:126 reveal-leads; brand.ts contextShifts proof-first; brand.ts outcomeFirst; onRamp entry-job outcome; Copy.tsx problem+outcome) become steps of one gesture. New canonical field `voice.heroOpen` (supersedes/renames `outcomeFirst`): "Open on the outcome, written as a reveal — the moment of new sight. Never open with the mechanism, the pain alone, or a platitude. Ground it next with a specific the reader can check; close by equipping the next step. The gesture adapts to any surface — a problem may set up the reveal, proof may carry the grounding — but the open always lands on the outcome. Exception: graph-native jobs…" Satellites harmonized (contextShifts.buyer = grounding step; onRamp = on-ramp specialisation; guide:126 = register note; Copy.tsx checklist corrected). Rationale accepted by Darren: the brand's own best copy (sanctioned hero, social, feature examples) already opens on an outcome written as a reveal; bare outcome-first flattens the personality, problem-first is the SaaS fear-lever, proof-first is impossible pre-launch. Implemented as v2.5.3. Consequence: the D27 exemplar rewrite must pass both the outcome-first structure and the reveal-register test.

19. **Marketing exemplar (D27): Draft A ratified (2026-08-08).** The canonical Marketing/Landing Page example — the few-shot AI tools imitate — becomes "See everything your organization knows in one connected view. Spectrea builds a living graph from your documents — the more you use it, the sharper it gets.", replacing the problem-first "Stop losing knowledge to silos…". Passes the heroOpen rule (outcome-as-reveal open, mechanism as reason to believe), keeps the compounding mechanism tie, sits at the two-privileged-words density cap ("see", "living"). The why-note stops teaching problem-first. Surfaced by the hero-open critic's round-1 finding 1; Draft B (problem-setup variant) offered and declined.

20. **Register taxonomies (D28 part 2): Option A — two structures, two jobs (2026-08-08).** `toneSpectrum` retired: it duplicated four `toneExamples` entries verbatim under colliding labels ("Marketing / Homepage" vs "Marketing / Landing Page", etc.); its tone labels moved into `toneExamples`, which becomes THE register taxonomy (6 content classes × tone + right/wrong + why; two labels authored to complete the set: Feature Announcement = Concrete + Confident, Beginner Documentation = Plain + Welcoming). `surfacePatterns` stays as the surface spec; new `voice.registerRule` declares the relation (a surface inherits its register from the nearest content class — System notifications ↔ Error Message, release notes ↔ Feature Announcement). Voice.tsx merges its two unrelated sections into one "Tone Registers — Voice in Action"; the guide's silently-merged table becomes the honest single table with tone labels; the checklist generator reads toneExamples. Implemented as v2.5.4. Options B (mega-merge) and C (labels-only) declined.

21. **Hybrid guide generation, wave 1 (decision 2 programme item, implemented 2026-08-08).** `public/brand-guide.md` is now a true hybrid: 19 generator-owned fenced blocks (`<!-- generated:ID -->`) rewritten from canon by `scripts/generate-guide.mjs` (`npm run generate:guide`, wired into `generate:all` before the PDF, which renders from the guide). Blocks: version header/footer (drifted 3× before), tone-registers table (drifted 2×), logo construction + clear space, accent states, washes light/dark, lifts, type system + minimum sizes + type scale + responsive (the scale/minimums/responsive data MOVED into `brand.typography` — previously guide-only), accessibility block (incl. measurement doctrine + exception registry rendered from entry data), motion durations/easings, radii/spacing/elevation. Prose outside fences stays hand-written; a hand edit inside a fence is overwritten by design. Generator verified idempotent (hash-stable across runs). **Wave 2 deferred (recorded):** §5 neutral/accent/Tailwind/dark-role tables (need canon colour-model expansion: OKLCH ladder, CSS vars, per-accent meanings), §6 gradient family, §11 component specs, §14 CSS-token block.

22. **Render-everything SPA build (decision 7 programme item, implemented 2026-08-08).** Every canon export that previously reached only agents (AI artifacts) now has a human-visible SPA surface, closing the F-03 inventory. New page **Trust & Disclosures** (`/communications/trust`, in Communications nav) renders all four `trustCopy` masters verbatim with the counsel note + trigger. **Voice & Tone** gains Surface Patterns (all 7 `voice.surfacePatterns` with rule + right/wrong) and Executive Voice (`executiveVoice` rule + example). **Naming** gains Company & Product (`naming.companyProduct`) and Origin (`originStance` — codified silence). **Positioning** gains the Full-Shape Claim (`positioning.fullShapeClaim`, internal north-star framing), the compounding `usageGuardrail` inline on its messaging card (also rendered on Copy & Taglines' messaging hierarchy), and the `differentiatorGuardrail` under the differentiation table. **Copy & Taglines** renders `voice.heroOpen` verbatim as "The Hero Open" ahead of the on-ramp it governs. **Primary Palette** gains Contrast Policy & Measurement — `accessibility.floor`, the measurement doctrine, and the exception registry rendered from entry data (both metrics, rationale, bounds). **Governance** opens with Source of Truth (`meta.sourceOfTruth`, `renderDoctrine`, version stamp, changelog) and closes with the full `ratificationLedger` rendered as a table — the F-02 divergence (page never mentioned brand.ts, the ledger, or versioning) is reconciled. Reconciliation fix: the two hand-written "never white text on Teal/Amber" don'ts (Primary Palette, Color Overview) now carry the ratified `semantic-button-labels-white` carve-out instead of contradicting it. `naming.aiNaming` was already rendered (v2.5.0 wave). Implemented as v2.5.6.

23. **D40/D35 accessibility cluster, wave 1 (implemented 2026-08-08).** Conformance fixes dictated by the ratified floors, plus one Darren-ratified colour: **(a) Tooltip keyboard path** — the "?" affordance is now a real `<button>` at the 24px WCAG 2.2 target size: tab-focusable with the canonical Amber ring, opens on focus, Escape dismisses, `aria-describedby` + `role="tooltip"`. **(b) Slider names** — the palette-compare range inputs carry `aria-label`s. **(c) Text floors** — ~90 sub-12px sites bumped to the 12px caption floor across 16 pages (10px button labels, 9px OKLCH readouts, 11px inline code, full sentences at 10px); the ~40 canon carve-outs at their sanctioned 10px (uppercase-tracked overlines, numeric badges, metadata chips) and the palette-explorer specimen mini-preview verified and left; four legal 11px uppercase overlines (PrimaryPalette, SemanticColors, BrandStory, Variations) were additionally normalized to 12px for consistency with the app-wide overline idiom — a deliberate tidy, not a floor fix. **(d) Pewter tier** — ~50 meaning-carrying sites stepped to Slate per pewterMatrix, including the Ghost button's light-mode label (was Pewter `#97979E` at 2.85:1 — an interactive label; spec table updated to "Slate / Stone 300") and the Iconography page's taught doctrine (secondary icon tier was Pewter, contradicting canon — now Slate, with an explicit rule reserving Pewter for disabled/decorative glyphs; dark ghost verified fine at ~5.8:1 on Ink). **(e) Teal/rose text tones — Darren ratified Option A** ("looks the same" — precisely its virtue): new canonical `brandTokens.accentText` = Teal text `#007D6E` (replaces `#008775` as text, which measured 4.04–4.45:1 vs the 4.5:1 floor; the new tone is 4.58–5.05:1, APCA Lc 68–74, visually near-identical) + Rose text `#BA3249` (names the shipping tone; the weaker `#D63B55` text uses at 4.13–4.55:1 swept to it). ~30 teal + 7 rose text sites swept; button state FILLS (`teal.active #008775`, `rose.hover #D63B55`) unchanged — fills are governed by the 3:1/exception-registry regime, not the text floor. Options B (reuse wash textOn `#0D5E56`, AAA but visibly heavier) and C (Ink headings) declined. Rendered evidence: decision-sheet teal-text panel (artifact be264ace). **Deliberately still open from D40:** the dark-mode Ink-on-lightened-Cobalt hover label (APCA Lc 40.5 despite WCAG-passing) — next adjudication; Motion-page alignment incl. `prefers-reduced-motion` stays D41.

24. **Dark Cobalt hover re-lightened (D40 closing item, ratified 2026-08-08, Option A).** The dark-mode transient idiom (label flips to Ink on a lightened fill) had exactly one state below the APCA spot-text line: Cobalt hover `#5C87E5` — WCAG 5.10:1 (passing) but APCA Lc 40.5 vs the 45 minimum, while Rose/Teal/Amber hovers sit at 46.7/61.7/~60. Same formula artefact as decision 17, opposite direction. Fix lightens one further notch along the existing ladder direction: **hover `#6E93EC`** (5.94:1, Lc 46.3 — parity with Rose), **active `#8FACF0`** (7.86:1, Lc 58.1). Base and all other accents unchanged. Consequential updates: buttonStates.dark.rule numbers, the exception-registry measured range (5.10–9.72 → 5.92–9.72 — a consequence, not a re-grant), Buttons.tsx state model/spec row/contrast note. Alternatives rejected: leave-as-is (defensible under the doctrine but leaves the one state its own adjudicator flags), white-label-on-hover-only (APCA 67.5 but WCAG 3.47 → exception + idiom break). Rendered evidence: decision-sheet dark-Cobalt panel. Implemented as v2.5.8. **This closes D40.**

25. **PDF pipeline determinism (D42, implemented 2026-08-09).** The brand-guide PDF was host-dependent: Lexend 600 rendered as synthetic bold, and the guide's seven symbol glyphs (Δ → ↔ ≈ ≤ ≥ ′) fell through to whatever fonts the build host supplied (Segoe UI / Segoe UI Semibold / Arial Bold on Windows). Three stacked causes, each isolated by controlled headless-Chrome print experiments: **(a) single-weight declarations of variable fonts** — the cached Google woff2s are full 100–900 variable files but were declared at one weight, pinning Chrome to the default instance and synthesizing every other weight; fixed by `font-weight: 100 900` declarations, with a verified print-pipeline constraint: all faces of one family must declare IDENTICAL weight descriptors or Chrome's print snapshot drops the base face. **(b) Latin subsets lack the symbols** — coverage is now split honestly across companion subset files under the same family names with disjoint unicode-ranges: Albert Sans/Lexend `text=` subsets supply their native Δ ≈ ≤ ≥; → ↔ ′ come from an Inter subset aliased under both families (Inter is the body stack's first fallback — the sanctioned substitute). Noto Sans was tried first and silently shipped a ′-only file: **Google's `text=` subsetter echoes the requested unicode-range while omitting glyphs the font lacks** (base Noto Sans has no arrows), so subset files are verified by glyph-coverage probe and the generator throws on any missing font file. **(c) the decisive hidden one: Google Fonts `@import` pollution from inlined SVGs** — `type-samples.svg`/`type-scale.svg` carry a `<style>@import` for standalone viewing; the PDF generator inlines SVGs, an inlined SVG's `<style>` joins the *document* stylesheet, and the imported single-weight latin-subset faces then win font selection at their exact weights and evict every non-latin glyph from the family (found by canary-paragraph auto-bisection of the rendered HTML: a paragraph whose Δ resolved in isolation fell back the moment the typography specimen entered the document). The generator now strips `@import`-bearing `<style>` blocks when inlining. Also in the wave: `orphans/widows: 3` and table pagination (`page-break-inside: auto`, `thead` as `table-header-group`, per-row `avoid`) for the orphan page gaps — 40 → 39 pages; and a `PDF_DEBUG_HTML` env affordance preserving the exact printed HTML for forensics. **Codex critic round 1 (revise) — four findings, all implemented:** JetBrains Mono moved to its variable 100–800 file with identical descriptors on both mono faces (the guide's four 600-weight code contexts — code spans inside a heading and three strong runs — now render a real 600 instance instead of synthetic bold); the SVG `@import` strip re-scoped per `<style>` block and made case-insensitive (the old regex could span from a benign style block across intervening SVG content into an importing one); a failed SVG inline now THROWS instead of silently reverting to the `<img>` path (which renders isolated with host fonts); and the generator gained a **post-print output gate** — it parses the produced PDF's font objects and fails the build unless every BaseFont/FontName/FontFamily EXACTLY matches a brand face (anchored family + instance-suffix pattern — a hostile `InterloperHost` cannot ride `Inter`), fails loud if zero font records are found (scanner/layout mismatch must not pass open), and glyph-bounds the dual-role families via their decoded ToUnicode CMaps: Inter faces may carry only ′ → ↔, and the platform colour-emoji face only 🎉 — so a HOST-installed Inter or an emoji face smuggling text glyphs also fails. The accepted emoji face's NAME is platform-dependent by design (Segoe UI Emoji on Windows, Noto Color Emoji on Linux, Apple Color Emoji on macOS — all three allowed, all identically glyph-bounded): the gate's first CI run proved this by correctly rejecting the Linux runner's Noto Color Emoji until the platform set was encoded, which is the gate catching real cross-host variance on its first contact with a second platform. Because Chrome's PDF splits identity and content across objects (descriptors carry names but no CMap; font objects carry the CMap but no name), the bound is applied through the font→descriptor object graph with identity resolved BEFORE any CMap look-up, and a bounded face with a missing, empty, or undecodable CMap fails closed (a stripped CMap cannot bypass the bound — byte-level mutant tests verify both paths). This closes the input-side blind spot (a readable-but-deficient subset passes every input check; only the output proves determinism). Negative-tested: the gate flags the pre-wave PDF's SegoeUI/SegoeUI-Semibold/Arial-BoldMT/Consolas, 13/13 name-anchoring cases behave, the live run reports 4 glyph-bounded faces checked, and a tightened-bounds mutation correctly flags the real Inter arrows (proving the bounds path executes); its first live run also caught Chrome's hyphenated variable-instance names (`JetBrains-Mono-Medium`) until the allowlist covered them. **Verified end state:** embedded fonts are exactly Albert Sans 400/600/700, Lexend 400/500/600, JetBrains Mono 400/600, Inter 400/600 (arrows), plus Segoe UI Emoji ×1 — the guide's three 🎉 anti-example occurrences, correctly host-rendered (colour emoji); font gate green. No canon change → **no version bump** (76672ed precedent). **Discovered, queued as an asset decision:** the specimen SVGs' own `@import` only ever works when a specimen is opened directly as a top-level browser document — every `<img>` context (markdown render, embeds) blocks external loads, so the specimens show fallback fonts there today; self-containing the two assets (embedded woff2 data URIs) vs dropping the import is a generate-brand-assets decision, and its FONT_IMPORT rationale comment (generate-brand-assets.mjs:565-568, "In the PDF pipeline the fonts are also loaded via a `<link>`") is stale either way. Codex critic gate: five rounds (r1 four findings incl. the JB Mono 600 synthesis; r2 gate hardening; r3 glyph-bounds dead code; r4 fail-closed ordering) → **accepted, no findings** (verdict: `.runs/2026-08-06-brand-review/pdf-pipeline-review/verdict.md`). **This closes D42.**

26. **Downloads surface + Social conformance (D43, Darren picked "build cheap versions too", implemented 2026-08-09).** The Downloads page's six "Coming soon" packages — Figma variables, Adobe ASE, PowerPoint/Keynote/Google Slides decks, HTML email, none of which exist anywhere in the repo — are deleted; the page now renders its asset listing from a **generated manifest** (`src/data/brand-assets-manifest.json`, written by generate-brand-assets.mjs), and the generator VALIDATES the contract both ways after writing it — every advertised path must resolve to a real FILE contained INSIDE `public/` (traversal paths that resolve outside are rejected before the existence check), and every file shipped into `public/brand-assets/` must be advertised, any miss failing the build — so "cannot advertise files that don't ship" is enforced, not asserted. That closes the 0/20 finding: all 20 SVGs are listed with previews (white variants presented on Ink) and individually downloadable, each link carrying an asset-named accessible label. **Cheap real packages built:** (a) **PNG logo exports** — the 8 marks/lockups rasterised at 1×/2×/4× (24 files) through the same headless Chrome the PDF uses, failing loud on any rasterisation error; backgrounds are faithful to the ratified SVGs: gradient/colour/Ink variants and the Gradient/White lockup are transparent, while the plain White mark and White lockup ship their Ink plates (the plate rects live in the ratified SVGs themselves — kept, recorded honestly in the manifest descriptions; the White-vs-Gradient/White inconsistency is flagged as a future asset decision); (b) **`public/spectrea-tokens.css`** — colour, type, radius, and spacing tokens as CSS variables, derived from brand.ts by ROLE (accents = `role === 'accent'`, neutrals = every other palette entry in canon order — the accent/neutral split is the only structure the generator declares, so any added or renamed palette entry propagates without touching the generator) and covering: spectrum + neutrals + washes/textOn, dark surfaces + dark washes + lifts, accent-text tones, button states light and dark (including the dark transient-label token), focus ring, font stacks, the type scale as size + line-height + weight variables per step, radii, and spacing; the page's CSS-tokens block now FETCHES this same file for display, replacing a hand-mirrored string — the shown CSS and the downloaded CSS are one artifact, and the generated version also carries the accent-text tones, wash textOn values, and focus-ring tokens the hand block lacked; (c) **`public/spectrea-tailwind.config.js`** — brand colours and font stacks as a `theme.extend` block, generated from canon. **Type specimens self-contained** (the D42-discovered asset item): type-samples.svg/type-scale.svg now EMBED the three brand fonts as data-URI woff2 subsets (printable ASCII + specimen punctuation; coverage-probed — never trust a `text=` subset's declared range) in place of the Google Fonts @import that never loaded in any `<img>` consumer; screenshot-verified rendering the true typefaces in both document and `<img>` contexts. The PDF inliner correspondingly strips @font-face rules (not just @import) from inlined SVG styles — inside the PDF document the same families are already declared with full coverage, and a subset face would shadow them; font gate green after regeneration. **Social conformance:** the "dark bg + gradient accent" Do-rule and Visual Templates tooltip re-scoped to the earned-Ink doctrine (Ink for quote and single-stat emphasis cards, Canvas elsewhere — the deck rule applied to the feed), and the engagement example's mechanism-free compounding claim now names the loop (source-attached connection, answers feeding the next) per the ratified usageGuardrail. tsconfig gains `resolveJsonModule` for the manifest import. No canon change → **no version bump**. Codex critic gate: three rounds (r1 — plate-dishonest "transparent" claims on the six white PNGs, name-enumerated/incomplete token inventory, unvalidated manifest, format-only link names; r2 — the replacement role ladder still re-declared canon structure, type-scale line-height/weight shipped as comments not variables, validation accepted traversal paths and directories, stale page screenshot) → **accepted, no findings** (verdict: `.runs/2026-08-06-brand-review/downloads-social-review/verdict.md`); the critic separately judged the white-PNG disposition sound — ratified plated SVGs untouched, records made honest, and the **White-vs-Gradient/White plate inconsistency deferred to Darren as a future asset decision** (the plain White pair ships on Ink plates, Gradient/White is white-on-transparent). **This closes D43.**

27. **Motion page conformance + logo animation canonized (D41, implemented 2026-08-09).** The Motion page contradicted canon on every axis the ledger named: it taught four duration tiers under invented names (Instant/Quick/Normal/Slow) against canon's seven (micro/standard/comfortable/deliberate/arrival/formation/spectrumSweep), claimed "two easing functions" against canon's three + never-rule, bound dropdowns/modals to ease-in-out against canon's ease-out entrance default (its modal demo even contradicted its own table), hand-drew easing plots matching neither curve (the "ease-out" plot was ≈cubic-bezier(0,.68,.32,1), the "ease-in-out" plot an ease-out shape), animated its signature-primitive demos on three non-canonical beziers, and showed a Cobalt focus ring that exists nowhere in canon — while documenting prefers-reduced-motion rules that src/ nowhere implemented. **The page now renders from canon** (`brandTokens.motion`): all seven tiers by canonical name, the three easings + never-rule verbatim, plots COMPUTED from the canonical control points (the plot region sized so elasticSettle's 1.56 overshoot renders honestly), demos on canonical curves with captions derived from the same values, even page chrome reading canon (every Tailwind transition utility replaced — Tailwind's defaults are exactly the shadow values the page exists to eliminate), and the focus-ring demo on the ratified Amber `.btn-focus` ring. **prefers-reduced-motion implemented app-wide**: index.css blanket block (durations, delays, iteration counts, scroll-behavior) plus a JS media-query check in AnimatedLogo — its rAF loop is invisible to CSS — which initialises AND holds the completed mark (no dots-only first paint). Screenshot pair proves it (`motion-check.png` mid-draw vs `motion-reduced.png` holding the finished mark). **Darren ratified canonizing the logo loop** (the critic's round-2 requirement: the signature animation's 3s/quadratic spec lived only in component code): new canon `logo.animation` — loopSeconds 3, phase boundaries (draw→0.57 quadratic ease-out, hold→0.60, dissolve→0.97 quadratic ease-in), trailingFadeFraction 0.15, reducedMotion behaviour, use — deliberately the logo's OWN spec outside the UI motion tokens; AnimatedLogo and the page's Signature Animation table both read it, and the contract emits it as data (`logoAnimation` — the 76672ed lesson applied at implementation time, not post-push). Demo correctness fixes from the critic: the duration bars and fade-in card previously mounted at their end state and only animated in reverse — both now remount at zero and animate forward (double-rAF). Propagation completed under critic pressure: the guide generator's motion blocks now emit all seven tiers and all three easings + never-rule; the guide's hand-written signature-primitives, animated-mark, and philosophy prose became three new generated blocks (22 total, hash-stable); PrimaryLogo/Variations derive the loop from canon and every explicit AnimatedLogo duration prop was dropped; use-guidance prose renders `logo.animation.use`/`reducedMotion` verbatim. **Canon change → version bump v2.5.9** + ledger entry. Codex critic gate: six rounds (r1 six findings incl. demos that only animated in reverse; r2 the ratification requirement Darren resolved; r3 guide/PDF propagation; r4 prose mirrors; r5 stale screenshots) → **accepted, no findings** (verdict: `.runs/2026-08-06-brand-review/motion-review/verdict.md`). **This closes D41.**

28. **Docs cleanup (D23, implemented 2026-08-09).** The audit's low-severity drift finding, resolved exactly along its own recommendation: **(a) three pure duplicates/backups deleted** (`matchwise-brand-audit (1).docx` — byte-identical to its sibling; `naming-proposal-validated.md.bak` — strictly older, zero "Spectrea" occurrences; `naming-proposal (1).md` — working draft superseded by the validated version); git history retains all three. **(b) The six remaining pre-rename artifacts moved to `docs/archive/pre-rename/`** (the naming sprint that recommended "Opvio", the metaphorical-candidates sweep, the validated proposal, the ranked list that placed Spectrea 35th, and the MatchWise audit .txt/.docx pair) with a README explaining the historical status and a `brand-critical-review.md`-style superseded banner prepended to every text file (the binary .docx is covered by the README and its .txt sibling). Zero references existed outside docs/ — re-verified by grep before moving. **(c) The three 2026-04-18 specs in `docs/superpowers/specs/` bannered as superseded**: the brand-iteration-v2 spec (which still asserted archetypes as three separate surface domains — inverted by current canon's "one character, not three"), the lowercase-wordmark/duotone-mark spec (wordmark/lockup/geometry re-ratified 2026-08-06, decisions 3–5), and — one step beyond the audit's two — the illustration-system-v3 spec, which the v4 prompt (2026-04-19) explicitly retires. **(d) The stale BA-10 comment in brand.ts corrected**: canon told every reader the lockup-form decision was "open" when it was ratified 2026-07-03 (option b, third form re-scoped into the gradient family); the comment now states the ratified outcome. Comment-only canon edit → **no version bump** (no value changed; regeneration verified — only the deterministic generation-date stamps advanced). `brand-critical-review.md` (already bannered) and `illustration-prompt.md` (live generator input) untouched. Codex critic gate: **accepted round 1, no findings** (verdict: `.runs/2026-08-06-brand-review/docs-cleanup-review/verdict.md`) — banner accuracy, reference integrity, and the date-only generated diff all independently verified. **This closes D23.**

29. **Governance small items sweep (D20/D22/D25/D29/D30, Darren ratified all four proposed items 2026-08-09, implemented same day as v2.6.0).** The audit's governance cluster, closed in one wave. **(a) D20 route half** — llms.txt links eight section-parent URLs (`/#/color`, `/#/logo`, …) that rendered the Not-found placeholder; App.tsx now declares eight `<Navigate replace>` redirects to each section's first page (redirect screenshot: `/color` lands on Color System overview). All 30 llms.txt leaf routes re-verified against App.tsx (count corrected from 25 by the critic). **(b) D20 canon half — gradient family canonized** as `brandTokens.gradients`, in the shape the 2026-07 audit itself recommended (AIF-25): `primary` (stops + OKLCH css + sRGB fallbackCss + interpolation note + use), `duets.cool/balanced/warm` (the Balanced Duet carrying the **`#6FB884` at 65% bridge** as a stop plus its bridgeRule), `fullSpectrumWithRose`, the adjacency rule, and the use/never lists. Before this, the bridge hex had NO canonical source — it lived in SpectreaLogo's hardcoded RGB lerp constants, Gradients.tsx markup, the asset generator's `BRIDGE` const, and guide prose, independently. Now: SpectreaLogo's four colour ramps are one stops-driven interpolator reading canon (rendered output verified identical — the six gradient SVG assets regenerate **byte-for-byte unchanged** from canon-derived stops); the generator derives every gradient strip from canon and throws if the bridge stop is missing; Gradients.tsx renders entirely from canon — which also surfaced and fixed a live page bug: its "Lockup gradient" swatch rendered a retired 5-stop full-spectrum ramp while its own caption said 2-stop Cool Duet (the caption was right; the swatch now renders `duets.cool`); and guide §6 became six generated blocks (gradient-primary/-recipe/-lockup/-full-rose/-duets/-rules), with the §3 colour-modes bullet reworded to stop hand-re-declaring the bridge value. One deliberate sub-perceptual page delta beyond the lockup fix (critic round 1, finding 3): the Full-spectrum-with-Rose page swatch previously used CSS-default thirds (33.33/66.67%); canon adopts the shipped SVG asset's ratified 33/66% stops, and the page now renders those — the asset stays byte-identical, the page shifts two inner stops by a third of a percent. This closes the guide-wave-2 §6 deferral early. **(c) D22 — package.json version sync**: generate-ai-formats now writes `meta.version` into package.json and package-lock.json's root entries whenever they differ (0.1.0 → 2.6.0 on first run) — the repo carries exactly one version, canon's, enforced by the pipeline rather than remembered. **(d) D25 — one canonical change process**: the guide §13 taught four steps carrying the version/regenerate mechanics while Governance.tsx taught five steps (Propose/Review/Test/Approve/Implement) with none of them and no cross-reference. Merged as canon `meta.changeProcess` — **Propose → Review → Test → Ratify & version → Regenerate & communicate** — preserving the five-step surface's Test stage and the four-step surface's mechanics (version bump + ledger entry + generate:all + #brand). Both surfaces now render the same field and cross-reference each other; severity explicitly sets each step's depth, never which steps happen. **(e) D29 — OS-frame worked example**: all seven prior occurrences of "the operating system for collective intelligence" were rule statements — the only sanctioned coined whole-product frame was the one voice rule with no applied copy. New canon `positioning.onRamp.osFrameExample`: right — *"See everything your organization knows in one connected view — Spectrea is the operating system for collective intelligence: the place where what your team knows connects, compounds, and answers back."* (outcome-first open; the frame arrives second; the plain-language clause carries the meaning, honouring the pairing rule); wrong — *"Spectrea: the operating system for collective intelligence."* (asks the frame to carry the claim alone — a coinRule violation — and leads with it, against the on-ramp's outcome-first posture). Rendered on Copy & Taglines as a right/wrong pair, in the guide as a generated block beside the on-ramp posture, and in brand-agent-rules as a few-shot line; the contract inherits it via the whole-onRamp export. **(f) D30 — already closed**: the 2026-08-07 ledger entry (name hygiene: sourced etymology, clean knockout search, formal clearance riding the counsel trigger) had closed it; only this doc's residual line was stale, now corrected — D30's clearance tail rides R9. **Canon changes → version bump v2.6.0** (a new token family + process doctrine + worked example) + ledger entry; guide regenerated (30 generated blocks, hash-stable), PDF font gate green, build green. Evidence: `.runs/2026-08-06-brand-review/governance-sweep-review/` (screenshots: gradients-check, governance-check, copy-check, redirect-color-check; negative tests: planted index.css drift mutations failed the build, a stale lock root-entry version was caught and resynced). Codex critic gate: three rounds. **Round 1 (revise, 6 findings)** — the two mediums completed single-sourcing: `selectedPalette.gradient` was itself a PARALLEL canon declaration (now derived from brandTokens.gradients.primary), ten page sites hardcoded the primary gradient (now the `.brand-gradient`/`-h` utility classes, with index.css promoted to an ENFORCED mirror — generate-brand-assets validates its four declarations against canon and fails the build on drift), and the lock sync skipped a stale `packages[""].version` when the top-level matched (now checks both); the lows: the full-rose page delta recorded as deliberate asset-alignment, the why-note's over-attribution fixed (claim-alone → coinRule; frame-leading → on-ramp posture), guide gradient rules rendered verbatim per-bullet instead of case-folded, and this decision's two count errors corrected. **Round 2 (revise, 3 findings)** — Downloads' quick-reference row and LogoExploration's spec table still hardcoded gradient strings (now render canon); the full-rose correction was formally brought into the brief's amended criterion 1; this decision's attribution phrase aligned to the corrected canon text. **Round 3: accepted, no findings** (verdict: `.runs/2026-08-06-brand-review/governance-sweep-review/verdict.md`) — the critic independently re-verified interpolation identity (0 mismatches across all four ramps incl. the t=0.5/0.65 boundaries), byte-identical gradient assets, route coverage, version convergence, and the negative-test claims. **This closes D20, D22, D25, and D29 (D30 having closed 2026-08-07).**

30. **Colour-model canonization — guide wave 2 §5 + §14 (Darren ratified full scope 2026-08-09, implemented same day as v2.7.0).** The colour doctrine that lived only in guide §5 prose and page code becomes canon: new top-level `colorSystem` export carrying the Warm Blend neutral ladder (seven role labels + the ladder rationale; entries key into `selectedPalette` by NAME — no hex re-declaration), the four accent meanings, the text-hierarchy tiers, the Tailwind→Warm-Blend mapping, the 60/20/10/10 usage ratio (light rows + dark rule), the Responsive/Structural/Semantic tier framework, the light-default rule, the named dark roles — Mist and Fog existed as shipped hexes (`selectedPalette.darkMode`) and tokens-file vars, but their NAMES, role mapping, and the why-two-tokens rationale had no canonical source — and the accents-on-dark rule. **Computed-vs-stored doctrine (D41 precedent extended): OKLCH lightness and WCAG contrast ratios are COMPUTED from the canon hexes by every consumer — generator, agent rules, and pages — never stored.** The doctrine immediately paid for itself twice: the computed ladder exposed two hand-rounding errors in the old §5 table (Slate OKLCH L 0.537 → 0.536, Iron 0.395 → 0.396 — critic-verified against independent math in all three rounds), and the round-1 critic proved the hand-written dark-token rationale's stored figures simply wrong (~6.3:1/~8.5:1 claimed; 6.10:1/8.20:1 computed) — that prose now carries no figures and its render sites append the computed values. The dark-role CSS var names (`--dark-canvas`…`--dark-fog`) are the one stored exception (not mechanically derivable; recorded as such in canon). **Surfaces:** guide §5's nine hand tables became generated blocks (neutral-ladder with computed OKLCH L + contrast columns, accent-meanings, text-hierarchy with computed conformance labels, tailwind-mapping, colour-ratio, dark-ratio, colour-tiers, light-default, dark-roles, dark-accents) and §14's abridged hand CSS fence now inlines the real generated `spectrea-tokens.css` — the shown, shipped, and downloaded CSS are one artifact (critic-verified byte-exact); the three colour pages render `colorSystem` instead of private copies (role chips, ratio bar geometry, dark-role table, tier cards — all canon-fed, with throw-guarded lookups); the asset generator derives the spectrum/neutral swatch rows and the ratio-bar geometry from the same data; the contract exports `color.system`; tokens-file dark-section comments derive from `darkRoles` rows. **Scope line (recorded in the brief):** palette-hex literals in page styling via the pre-existing `selectedPalette` lookup idiom are the established app-wide pattern and were out of scope — only the newly canonized `colorSystem` data required single-sourcing. **Canon changes → version bump v2.7.0** + ledger entry (amended post-gate to record the critic-round deltas); version sync propagated 2.6.0 → 2.7.0 into package.json + lock. Codex critic gate: three rounds. **Round 1 (revise, 6 findings — 1 high, 5 medium):** the high was the stored-and-wrong dark-role ratios (fixed at doctrine level: figures deleted from canon, computed at render); the mediums: the Primary Palette ratio bar rendered an 8%/12% split contradicting canon's 10/10 (now derives from `ratio.light` with the spectrum segment split across accents, mirroring ratio-bar.svg), `ratio.darkRule` was canonized but unconsumed while the guide kept a hand copy (new `dark-ratio` generated block), page contrast figures were still literal (now computed via `CANVAS_RATIO`/`ON_INK` helpers), Primary Palette's role chips were private case-folded labels ("hero", "spectrum" — now verbatim `roleLabel`/`darkRoles.role` via throw-guarded lookups), and the tokens-file generator case-folded canon strings in comments (the `lc()` helper deleted entirely — §14 inlines the file into the guide, so folded labels were paraphrased canon; type-scale/radii/spacing comments included, beyond the flagged line). **Round 2 (revise, 3 medium):** `accessibility.contrast.tokens`/`accentsOnInk` still stored palette-derived figures and duplicated `colorSystem.accentsOnDark` — both DELETED from canon (the D10 Cobalt correction preserved via `accentsOnDark`; guide bullet, agent-rules line, and the page's Contrast Policy paragraph all compute the ladder now, the AI-formats generator gaining its own WCAG helpers); Semantic Colors and Primary Palette paraphrased `accentsOnDark` around computed figures (both now render it VERBATIM with a separate computed sentence; the private table-header summary became a neutral note); Palette Explorer paraphrased the ratio `what` phrases (now interpolated verbatim). The grant-time records boundary was recorded: exception-registry `measured` values, the focus-ring note, accent-text `use` strings, pewterMatrix.principle, and ledger entries record measurements AT DECISION TIME per `accessibility.measurement` — records, not live claims. **Round 3: accepted, no findings** (verdict: `.runs/2026-08-06-brand-review/colour-model-review/verdict.md`) — the critic independently reproduced the full OKLCH ladder and every spot-checked ratio, confirmed §14 byte-exactness, 41 unique generated markers, version convergence, and clean tsc. Evidence: `.runs/2026-08-06-brand-review/colour-model-review/` (brief with amended-scope history, per-round verdicts + fix notes verbatim, three page screenshots retaken after every fix round). Pipeline: `generate:all` green and hash-stable, PDF font gate green, build green. **This closes guide wave 2's §5 and §14; §11 component specs remains its own wave.**

31. **Component specifications canonized — guide wave 2 §11 (Darren ratified full scope 2026-08-09, implemented same day as v2.8.0).** The component doctrine that lived only in guide §11 prose and four page files becomes canon: new top-level `components` export (between `colorSystem` and `accessibility`) carrying **buttons** — the six types (Primary/Secondary/Ghost/Destructive/Confirm/Caution) with tier (Hierarchy/Semantic), palette and state-group references by NAME (`palette: 'Cobalt'` keys into `selectedPalette`, `stateKey` into `buttonStates` — no hex re-declaration), label colours, roles, treatments, Confirm/Caution reservation details + example lists, the one-Primary-per-section rule, three sizes (Compact 32 / Default 36 / Large 48 px with padding, font size, and radius by TOKEN name), the Lexend Medium 500 button font, the disabled treatment, and the light-mode hover-direction rule; **forms** (36/32 px heights, stone-200 border, `lg` radius token, padding, font, Pewter placeholder, focus = ratified ring reference, Rose error, Cloud disabled fill); **cards** (stone-200 border with stone-300 hover, `xl` radius + `lg` padding tokens, Canvas background + surface note, elevated variant, `md` gap, title font); **layout** (256 px sidebar, 40–48 px top bar, Cloud content area, the four breakpoints with column counts, `md` grid gap, three content widths, the borders-not-shadows elevation rule, and six responsive rules incl. the 44 px touch-target floor). **Drift correction folded in:** two component pages taught `#E5E7EB` — Tailwind *gray*-200 — as the border family while the shipped UI uses *stone*; canon ratifies stone-200 `#E7E5E4`/stone-300 `#D6D3D1`, and consumers declare the Tailwind values as commented constants explicitly marked "Tailwind values, not canon". **Surfaces:** guide §11's hand prose became six generated blocks (buttons-types — which VALIDATES every type's stateKey against both button-state modes before rendering base/hover/active values for light and dark; buttons-spec; focus-ring; forms-spec; cards-spec; layout-spec) — 47 generated blocks total, hash-stable; the contract exports top-level `components`; and the four component pages (Buttons, Forms, Cards, Layout) render canon through throw-guarded lookups — spec tables, size cards, semantic reservation cards, state tables, breakpoint/content-width/responsive tables, elevation convention, and the focus demos all canon-fed. **Verbatim doctrine extended to instructional chrome:** page tooltips and Do/Don't lists that paraphrased canon rules were rewritten to compose from canon strings verbatim (the Sizes tooltip renders every size's `use` string; three Don't items restating the one-Primary/semantic rule were deleted — the rule renders verbatim once). **Reference-integrity lesson (the gate's through-line):** consumers must branch on SHAPE, not name — a type carrying `stateKey` without `palette` is a fill/text-shaped state group, dereferenced purely through `t.stateKey` (the guide branch shape-guards the dereferenced group and throws if it is not fill/text-shaped) — so a canonical key rename flows through both renderers or fails loud; no literal key-name comparison remains. **Canon changes → version bump v2.8.0** + ledger entry; version sync propagated 2.7.0 → 2.8.0 into package.json + lock. Codex critic gate: four rounds, findings narrowing 4 → 2 → 1 → 0, all in the reference-integrity/verbatim class. **Round 1 (revise, 4 medium):** Secondary's stateKey was validated but its values never rendered; the focus-ring block omitted the dark ring value and paraphrased the light one; the guide resolved the Default size's radius via a hardcoded `'lg'` instead of `DEFAULT_SIZE.radiusToken`; tooltips and Do/Don't items paraphrased canon doctrine. **Round 2 (revise, 2 medium):** Secondary rendered through hardcoded `.secondary` property paths instead of dereferencing the validated `t.stateKey`; the Sizes tooltip truncated canon `use` strings and the Don't list restated the rule. **Round 3 (revise, 1 medium):** both consumers still GATED the branch on the literal key name (`t.stateKey === 'secondary'`) — a valid rename would pass validation but fall through to dash output. **Round 4: accepted, no findings** (verdict: `.runs/2026-08-06-brand-review/component-specs-review/verdict.md`) — the critic confirmed the structural branch in both consumers, re-verified version convergence (package/lock/lockRoot/contract 2.8.0, componentKeys buttons/forms/cards/layout, 6 button types, 6 responsive rules), generated-block integrity, and the refreshed render evidence. Evidence: `.runs/2026-08-06-brand-review/component-specs-review/` (brief, per-round verdicts + fix notes verbatim, four page screenshots retaken after every fix round). Pipeline: `generate:all` green and hash-stable, PDF font gate green, build green. **This closes guide wave 2 entirely — §5, §6, §11, and §14 are all generated.**

32. **White assets ship both dispositions (D43 residual, Darren ratified 2026-08-10, implemented same day).** The asset family carried two philosophies: the plain White mark and White lockup ship with an Ink plate baked into the ratified SVG (inherited by their PNGs), while the Gradient/White lockup is white-on-transparent. Darren picked **"ship both"** over making the pair transparent (recommended) or keeping plates only: the plated files stay byte-unchanged as the self-visible defaults, and new **`logo-mark-white-transparent.svg` / `logo-lockup-white-transparent.svg`** companions (plus 1×/2×/4× PNGs — 8 new files) join the catalogue for placement on the consumer's own dark surface. Geometrically identical to their plated counterparts minus the plate rect; manifest entries distinguish the two dispositions honestly; the Downloads page previews the new items on Ink automatically (white-name rule); the generator's both-ways validation now covers 54 advertised paths. Purely additive — every pre-existing generated artifact regenerated byte-identical; no canon value changed → **no version bump** (generator-only precedent). Codex critic gate: **accepted round 1, no findings** (verdict: `.runs/2026-08-06-brand-review/white-transparent-review/verdict.md`) — the critic independently verified SVG parity (plated minus only the rect), pixel-checked all six PNGs for transparent corners and zero plate pixels, and confirmed the 54-path manifest bijection and canon/version/tokens unchanged. Folded into the same wave: `docs/brand-review-2026-08/downstream-handoff.md` refreshed from its stale v2.5.0 framing to v2.8.0 (re-pin sources now include spectrea-tokens.css, the Tailwind snippet, the PDF, PNG exports, and the transparent pair; contract sections and copy-affecting rules added since v2.5.0 enumerated). **This closes the D43 White-plates residual.**

33. **Voice humanist correction + stone chrome sweep (Darren flagged both 2026-08-10, ratified option B of three, implemented same day as v2.9.0).** Darren's review of the live guide surfaced two issues: the Forms page's email input showed "an additional darker line" versus its neighbours, and Voice & Tone read "a little heavy … 'earning the jargon' causes bloat which is not humanistic". Both diagnoses held. **(a) Voice doctrine:** the formula ("…Tech earns its place by *showing its work*") was being applied as a COMPOSITION rule — nearly every marketing-register example welded a mechanism clause onto its claim (the em-dash tail as a tic; the Feature Announcement example proved compounding three sentences in a row) — over-weighting Grounded, under-weighting Warm. Ratified rewrite (option B, deepest of three offered): **`voice.formula` = "Plain words, real specifics, room to breathe. Tech earns its place by being checkable — shown once, where the reader looks for it."** — the anti-black-box stance survives (checkable ≥ shown) while the per-sentence pressure goes; new canon **`voice.attentionRule`** makes the discipline explicit ("earn the jargon" is an ADMISSION rule, not a composition rule; one claim, one proof per surface; mechanism one step from the claim; the claim—dash—mechanism shape is a tool, not a rhythm); `techDescription` harmonized to "being checkable"; `techApproach` ("Earn the jargon") deliberately unchanged as the admission rule's name; three examples retuned (Feature Announcement → *"Connections you never noticed start surfacing. Your second month is better than your first."*; Beginner Documentation dropped the tail restating its own sentence; Settings unstacked its nested clause chain); the D27 hero exemplar unchanged — a hero carries exactly one claim + one mechanism, already passing once-per-surface. The reveal→ground→equip triad re-anchored to the heroOpen gesture (the formula states the texture) in brand.ts's archetype note and the guide's §1/§2 prose. Propagation: new `voice-formula` generated block; the guide's hand Surface-patterns table became the `surface-patterns` generated block (49 blocks total — the round-1 High finding, fixed at class level); the contract emits `vocabulary.formula`/`attentionRule` for the first time; agent-rules, checklist (formula + attention lines), llms.txt, few-shots, and the Voice page (attention-rule card replacing the stale hand echo) all render verbatim; the only surviving old-formula copies are deliberate history (ledger + provenance comments). **(b) Stone chrome sweep (conformance to decision 31's ratified stone family, not a new decision):** Darren's darker-line sighting was the email input's inline Tailwind **gray**-200 border beside stone-200 neighbours — plus its focus never turned Cobalt (inline style beat the classes). All ~40 gray-family chrome sites across 22 page files swept to stone (`#E5E7EB`→`#E7E5E4`, `#D1D5DB`→`#D6D3D1`, `#F3F4F6`→`#F5F5F4`, case-insensitively after round 1 caught a lowercase remnant); the email input's neutral state is now class-driven (`border-stone-200 focus:border-brand`), pixel-consistent with its siblings, validation states keeping their 2 px semantic borders; the Ghost button demo now matches its own stone-300 spec. **Canon changes → version bump v2.9.0** + ledger entry; downstream handoff gained the voice bullet + v2.9.0 refs. Codex critic gate: two rounds. **Round 1 (revise — 1 high, 2 medium):** the guide's hand surface-patterns mirror still carried the old Settings example (→ generated block); the checklist omitted the formula; one lowercase gray hex survived the case-sensitive sweep. **Round 2: accepted, no findings** (verdict: `.runs/2026-08-06-brand-review/voice-humanist-review/verdict.md`) — verified through to the BUILT bundle (dist JS carries the new formula, retuned strings, and stone hexes). Pipeline: generate:all green, guide hash-stable at 49 blocks, PDF font gate green, tsc + build green.

34. **One focus system — the canonical Amber ring on every focusable (Darren flagged 2026-08-10, ratified after adjudication + comparison, implemented same day as v2.10.0).** Darren's observation: "it is inconsistent why focus rings are yellow but text fields focus is blue" — buttons carried the ratified 2 px Amber ring while inputs turned Cobalt border + 20% Cobalt halo — plus two direct questions: would unifying on blue clash with existing blue usage, and is Amber too synonymous with warning? Both concerns held against the numbers: the input halo (`ring-brand/20`) measured **1.29:1 against white — functionally invisible** (the working indicator was really the border-colour swap, a channel validation also uses), and a Cobalt ring on the Cobalt Primary button measures **1.00:1** — the flagship button would have an invisible focus state, so blue DOES clash with existing blue. Amber-as-warning: defused by transience — a focus ring appears only while attention is literally at the element and moves away with it; it never sits still long enough to read as a status, and canon now says so. **Adjudication (framing-blind):** four candidates went to the Codex critic with dimensions but WITHOUT my recommendation (D, an Ink ring) — the critic independently chose **A: the existing Amber ring everywhere** (transience defuses the warning collision; Ink would double-book the structural channel; least churn — the ring, its dark variant, and its `.btn-focus` plumbing already existed and were already ratified); I conceded, and after a pros/cons + industry-comparison round (GitHub/Stripe-school brand-accent ring vs. system-blue school; delivered via artifact + option previews after a display miss), **Darren ratified A**. Amber ring contrast: 4.21:1 (white) / 3.89:1 (Cloud) light, ~4.0:1 dark (3.73 Graphite, 3.30 Fog vs. the 3:1 non-text floor). **Doctrine (new `brandTokens.focusRing.rule`):** ONE ring for every focusable — buttons, links, inputs, selects, textareas, custom controls; the ring is dedicated accessibility chrome OUTSIDE the colour tiers — an ephemeral attention-is-here signal, never a validation status or decorative accent; **borders never change on focus** — validation borders stay visible beneath the ring. Tier-1 Responsive drops focus from Cobalt's job list ("Focus indication is not a Cobalt job"); graphViz's `nodeHoverFocus` split into `nodeHover` (Cobalt, Tier-1) + `nodeFocus` (the canonical ring) — the last second focus identity in canon. `components.forms.focus` rewritten to reference the ring; the Buttons page's "never on click" focus-visible claim corrected (browsers may show the ring after pointer activation on text inputs). **Implementation:** Forms inputs/select/textarea swapped `focus:border-brand focus:ring-brand/20` + `outline-none` for `.btn-focus`; checkboxes and radios became real keyboard controls — visually-hidden native inputs (`.sr-input`) with the ring on the adjacent styled box via a new `.focus-proxy` CSS rule — and the toggles gained `role="switch"` + `aria-checked` + the ring; a brace-aware JSX inventory then found **28 more enabled focusables without the ring**, all fixed (Sidebar home link, PaletteCompare range slider, PaletteExplorer deep-dive cards, Buttons copy chip, Cards alert/tag actions, Motion demo controls, LogoExploration's shared select class, Downloads cards + download chips); the four static "Focused" paint-chip specimens left the tab order (`tabIndex={-1}`); and the mobile sidebar — whose offscreen links had remained tabbable while invisible — became a real drawer (`inert` when closed below lg via a shared `useIsDesktop` hook; Escape closes; opening focuses the drawer's first ringed link and closing restores the opener; while open on mobile, App marks the obscured TopBar+main content `inert`, so Tab is contained in the drawer). Six stale page teachings of the old Cobalt-focus doctrine swept (SemanticColors ×3, PrimaryPalette, PaletteExplorer, Iconography), and the guide's hand graph-semantics list — the last surface still teaching "Node hover/focus: Cobalt" — became the `graph-viz` generated block, whose builder throws on any unlabelled canon key (the decision-33 hand-mirror lesson applied again at class level). **Canon changes → version bump v2.10.0** + ledger entry (adjudication history, the 1.29:1 halo finding, and the tier contradiction recorded); guide `focus-ring` + `graph-viz` blocks render the doctrine verbatim (50 blocks, hash-stable); contract carries `focusRing` whole-object. Codex critic gate: framing-blind adjudication (`adjudication.md`) + implementation rounds. **Round 1 (revise — 4 major):** six stale Cobalt teachings; the graphViz second identity; the 28 unringed focusables; the mobile-sidebar focus leak. **Round 2 (revise — 2 major):** the guide's hand graph list still taught the merged identity (→ generated block); the OPEN drawer let Tab escape into the obscured content and its programmatic focus target (the bare aside) had no ring (→ content-side `inert` + first-link focus). **Round 3 (revise — 1 medium, evidence-freshness only):** both substantive round-2 fixes passed (the critic independently verified the eight-key graph-map exhaustiveness, guide/contract/PDF parity at 50 marker pairs, zero inventory misses, drawer containment — including a spec-level check that native `inert` does not clear active focus synchronously, so opener restoration is sound — and v2.10.0 convergence); the sole finding was that the review patch had gone stale against the working tree because this doc's own entry advanced mid-review — regenerated with the doc's final pre-acceptance state. **Round 4: accepted, no findings** (verdict: `.runs/2026-08-06-brand-review/focus-system-review/implementation-verdict-round4.md`; per-round verdicts + fix notes verbatim in the same directory) — the critic re-verified clean reverse-application of the evidence patch, 50 paired generated blocks, zero stale hover/focus teaching, guide/contract parity, and fresh tsc. Pipeline: generate:all green, PDF font gate green, tsc + build green.

35. **Consumer conformance checking, and canon gains a retired-values register (Darren ratified 2026-08-11; checker built same day, register implementation pending).** Origin: the 2026-08-11 downstream drift scan was hand-written and looked for logo geometry, focus styling and stale pins — it never looked at copy at all, and the category noun retired 2026-07-03 (VM-F-CAT) was found still live in spectrea's site-wide metadata, its `(app)` layout and its OG image, five weeks later. Ratifying canon and shipping it downstream is worth little if nothing downstream can detect drift back. **(a) The checker.** `scripts/generate-conformance.mjs` reads `src/data/brand.ts` and emits `public/brand-conformance.mjs` — a zero-dependency Node CLI consumers vendor with the snapshot and run in CI (`node docs/brand/brand-conformance.mjs src public --strict`); engine source is `scripts/conformance-engine.js`, wired into `generate:all`. Validated against real history: over the three pre-fix files (`git show 081052028^:…`) it reports all three retired-noun escapes as errors; over both consumer repos at current state, zero errors. **Two design decisions carry it.** *Severity is load-bearing:* canon's `badSubstitutions` mixes the specific retired noun with generic words like "AI assistant", and Spectrea genuinely has an AI assistant feature — run undifferentiated the checker produced **82 errors in the product repo, 80 of them noise** ("AI Assistant Capture Flow" in a test name, "what is knowledge management" as a retrieval query). Generic entries are therefore `review`, only the retired noun is `error`; the same split applies to `forbiddenVerbs` ("handles it for you" is `error`, "takes over" is ordinary English in focus-handling comments). A checker whose error tier cries wolf is switched off within a week. *Canon growth fails the build rather than escaping the checker:* every canon entry carrying a parenthetical is classified explicitly in the generator — `reason` (always a violation) vs `condition` (a human judges) — and an entry it has not been taught is a hard generator failure, the same defensive pattern as decision 33's guide-block builder. One canon entry is deliberately NOT checked and says so in the generated header: lowercase "spectrea" in prose, indistinguishable from package names, domains, import paths and URLs. Suppression exists at line and file level and **requires a stated reason**; a bare suppression is itself an error. **(b) The register — the decision Darren made.** The checker cannot enforce a migration rule unless canon holds the retired value: canon carries the *current* focus ring, so nothing generated from canon can recognise the old one — which is precisely why the focus-ring migration had to be a hand scan. The category noun is checkable only because `categoryGuard.badSubstitutions` already keeps its retired forms; that field is a retired-values register with one entry. Options put to Darren: **A** canonize a general register, **B** keep canon present-tense and hold retired values in the generator, **C** leave it at the category noun. **Darren ratified A.** Rationale carried in the recommendation: A generalises a pattern already ratified rather than inventing doctrine, and B would recreate exactly the hand-mirroring that decision 4 eliminated (R10/D8) — a second list outside canon that goes stale silently. Accepted cost: canon carries history alongside truth, and each retirement becomes two steps (change the value, record the old one). **Also surfaced for consumers:** spectrea hard-codes the never-use vocabulary across seven test files — a hand-copied list with its own silent drift risk, and what the generated checker is meant to replace.

## Still open

_Reconciled 2026-08-11._ This section had been carrying items that later decisions
closed, and it reported Pages HTTPS as unenforced when it is enforced. Every closure
below was re-verified against the decision it names and against canon or a live API
read — not against the earlier text of this section. The ledger of what closed is
kept beneath the open list so the D-number cross-references still resolve.

### Genuinely open

- **The retired-values register (decision 35b)** — ratified 2026-08-11, not yet
  implemented. Canon gains a register of retired values so migrations become
  machine-checkable downstream the way the category noun already is; the
  conformance checker then reads it. Blocked only on the checker's critic gate
  clearing, since implementing it regenerates the file under review.
- **The art session** (Darren schedules; it is one session, not five items) —
  illustration under canon (D4/D6/D7/D33/D32; P2), hero-anchor **regeneration** (P4:
  decision 9 shipped the honest caption as the interim; the anchor itself is still the
  pre-canon render), and F6 style-family opinion.
- **R7, graph-viz operational spec** — open **by design**, not by neglect: canon states
  at `brand.ts:1157` that the operational graph-rendering spec is a product design
  deliverable, not a brand-guide one. It closes in the product repo or not at all.
- **The counsel trigger** — a standing control rather than an open decision. Decisions
  13 and 14(c) route trust-copy review and formal trademark clearance to one engagement
  fired before any public launch that puts trust/security/compliance claims on a page.
  Nothing is owed until that trigger fires; the knockout search that precedes it ran
  2026-08-07 and found no blocker.
- **Downstream D31 analogue (R11)** — D31 is closed *here* (guardrail canonized at
  `brand.ts:134`/`:226`, guide §"Compounding Intelligence" names the mechanism), but the
  2026-08-11 drift run found bare "compounding intelligence" without its mechanism on
  three consumer surfaces. Tracked as R11 in
  `.runs/2026-08-11-downstream-drift-fix/`, owned by the consumer repos.

### Closed since this section was first written

| Item | Closed by | Verified |
|---|---|---|
| Metaphor hierarchy (D24) | decision 6 — Prism and Living Network co-primary | decision text |
| Naming.tsx registers (D18), unrendered doctrine layer (D26/D19) | decision 7 (render everything), implemented decision 22 | decision text |
| Exemplar outcome-first (D27) | decision 19 — Draft A ratified | decision text |
| Hero-opens and taxonomies (D28) | decisions 18 (part 1) and 20 (part 2) | decision text |
| Compounding mechanism (D31) | decision 12 — mechanism guardrail | `brand.ts:134`, `:226` |
| Contrast policy (D10/D11/D34) | decision 8 + exception rounds 16 and 17 | decision text |
| Accessibility cluster (D35/D40) | decision 23 (wave 1); its one deliberate carry-over, the dark Cobalt hover label, closed by decision 24 | decision text |
| On-wash text hexes (D36) | canonized 2026-08-06 | `brand.ts:767–776`, `brand-contract.json` |
| 9px sightings | decision 23(c) text-floor sweep | decision text |
| R8 (VM-73) | decision 12 — DEFEND, keep the phrase | decision text |
| R9 (counsel review) | decision 13 — closed as controlled; see the counsel trigger above | decision text |
| R10/D8 (generator hand-mirrored canon) | decision 4's generator rewrite — generators now import `brand.ts` | `generate-guide.mjs`, `generate-brand-assets.mjs` |
| **Pages HTTPS** | already enforced — no go-ahead needed | `gh api …/pages` → `https_enforced: true`, cname `branding.spectrea.com` (read 2026-08-11) |
| Governance small items (D20/D22/D25/D29/D30) | decision 29 | decision text |
| PDF pipeline (D42) | decision 25 | decision text |
| Downloads/Social (D43) | decision 26 | decision text |
| Motion page (D41) | decision 27 | decision text |
| Docs cleanup (D23) | decision 28 | decision text |
| Guide wave 2 | §5+§14 decision 30, §11 decision 31 — fully closed | decision text |
