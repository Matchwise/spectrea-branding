import PageShell from '../../components/layout/PageShell'

export default function Photography() {
  return (
    <PageShell
      title="Abstract Imagery"
      subtitle="Visual language for backgrounds, empty states, and branded moments — rooted in the prism and living-network metaphors."
    >
      <div className="flex flex-col items-center justify-center h-64 rounded-xl border-2 border-dashed border-stone-200">
        <p className="text-stone-400 text-sm">Visual showcase in progress.</p>
        <p className="text-xs text-stone-300 mt-2">Will include full-size mockups of dot networks, prismatic shapes, and gradient washes in real contexts (heroes, empty states, social cards, slides).</p>
      </div>
    </PageShell>
  )
}
